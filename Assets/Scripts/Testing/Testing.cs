using Units;
using UnityEngine;

namespace Testing
{
    public class Testing : MonoBehaviour
    {
        [SerializeField] private Unit unit;
        
        private void Update()
        {
            if (Input.GetKeyDown(KeyCode.T))
            {
                
            }
        }
    }
}